<%= form_with(model: ingredient_set, local: true) do |form| %>
  <% if ingredient_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ingredient_set.errors.count, "error") %> prohibited this ingredient_set from being saved:</h2>

      <ul>
      <% ingredient_set.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :ingredient_set_title %>
  </div>

  <div class="field">
    <%= form.label :position %>
    <%= form.number_field :position, id: :ingredient_set_position %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
